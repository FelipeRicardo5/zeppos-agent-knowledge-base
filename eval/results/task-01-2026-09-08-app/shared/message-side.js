/*
 * BLOCKED FILE — same block as shared/message.js, Side Service half.
 * patterns/bluetooth-communication.md names it ("/shared/message-side.js") and
 * uses it, but the KB carries no source and no API surface for it. Gap G02.
 */
throw new Error(
  'shared/message-side.js (MessageBuilder, side) is not covered by the knowledge base — see gap G02'
)
